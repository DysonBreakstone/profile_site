import React from "react";
import profilePhoto from "./headshot5.jpg";

export default function ProfilePanel() {
  return (
    <div id="profilePanel" style={{ backgroundImage: `url(${profilePhoto})`}} className="w-full h-full rounded-lg bg-cover">
      
    </div>
  );
};